publish-npm:
  image: node:22
  rules:
    - if: $CI_COMMIT_TAG
  id_tokens:
    NPM_ID_TOKEN:
      aud: "npm:registry.npmjs.org"
    SIGSTORE_ID_TOKEN:
      aud: sigstore
  script:
    - npm ci --ignore-scripts
    - npm publish --access public --ignore-scripts

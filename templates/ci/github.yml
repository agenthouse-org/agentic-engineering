name: agenthouse evaluation
on: [pull_request]
permissions:
  contents: read
jobs:
  evaluate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      # Add the reviewed framework tarball as a locked project dependency first.
      # npm ci honors the configured enterprise registry and certificate settings.
      - run: npm ci --ignore-scripts
      - name: Restore the exact project runtime into the machine cache
        run: node node_modules/@agenthouse/engineering/bin/ah-engineering.js restore
      - name: Evaluate
        run: node .agenthouse/run.mjs evaluate --profile pull-request --ci --frozen --subject "${{ github.sha }}"
      # Includes result.json, junit.xml, report.html and linked evidence.
      # GitHub stores these as downloadable artifacts; arbitrary JUnit XML is
      # not parsed into a native test-results view without a reviewed reporter.
      - name: Preserve evaluation and test reports even when evaluation fails
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: agenthouse-evaluation-reports
          path: .agenthouse/local/reports/
          include-hidden-files: true
          if-no-files-found: warn
